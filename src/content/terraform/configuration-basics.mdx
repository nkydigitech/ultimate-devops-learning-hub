---
title: "Configuration & Variables"
description: "Write clean, reusable Terraform: resources, input variables, outputs, and how to reference one resource from another."
order: 2
---

import LessonHeader from "../../components/LessonHeader.astro";
import CommandBlock from "../../components/CommandBlock.astro";
import Terminal from "../../components/Terminal.astro";
import Tip from "../../components/Tip.astro";
import Warning from "../../components/Warning.astro";
import Quiz from "../../components/Quiz.astro";
import InterviewPrep from "../../components/InterviewPrep.astro";
import Exercise from "../../components/Exercise.astro";
import NextLesson from "../../components/NextLesson.astro";

<LessonHeader
  title="Configuration & Variables"
  description="From hard-coded values to clean, reusable, parameterised infrastructure."
  timeToRead="11 min"
  icon="🏗️"
  section="Terraform"
  sectionUrl="/terraform/introduction"
/>

Hard-coding values everywhere makes configs impossible to reuse. Terraform gives you **variables** for inputs, **outputs** for results, and **references** to wire resources together — so one clean config can build many environments.

---

# 🎯 Learning Objectives

By the end of this lesson you will:

- Define and use input variables
- Return useful values with outputs
- Reference one resource's attributes from another
- Understand `terraform.tfvars` and variable precedence

---

# Input Variables

Variables turn hard-coded values into parameters:

<CommandBlock command={`variable "region" {
  type    = string
  default = "us-east-1"
}

variable "instance_count" {
  type    = number
  default = 2
}

variable "environment" {
  type = string
  # no default → Terraform will prompt or require a value
}`} language="hcl" />

Use them with `var.<name>`:

<CommandBlock command={`provider "aws" {
  region = var.region
}`} language="hcl" />

<Tip title="Type your variables">
Always give variables a `type` (string, number, bool, list, map). It catches mistakes early and documents what each variable expects.
</Tip>

---

# Supplying Variable Values

Terraform reads variables from several sources, in order of precedence (later wins):

| Source | Example |
|--------|---------|
| Defaults in the `variable` block | `default = "us-east-1"` |
| `terraform.tfvars` file | `region = "eu-west-1"` |
| `-var` on the command line | `terraform apply -var="region=ap-south-1"` |
| Environment variables | `TF_VAR_region=us-west-2` |

A typical `terraform.tfvars`:

<CommandBlock command={`region         = "eu-west-1"
instance_count = 3
environment    = "production"`} language="hcl" />

<Warning title="Keep secrets out of tfvars in Git">
Never commit real credentials or secrets in `.tfvars`. Use environment variables (`TF_VAR_...`) or a secrets manager, and add sensitive `.tfvars` files to `.gitignore`.
</Warning>

---

# Outputs

Outputs surface useful values after an apply — like a server's IP or a bucket's name:

<CommandBlock command={`output "bucket_name" {
  value = aws_s3_bucket.data.bucket
}

output "bucket_arn" {
  value = aws_s3_bucket.data.arn
}`} language="hcl" />

<Terminal
  command="terraform output"
  output={`bucket_arn  = "arn:aws:s3:::my-devops-course-bucket"
bucket_name = "my-devops-course-bucket"`}
/>

Outputs are also how one Terraform configuration hands values to another.

---

# Referencing Resources

The real power: use one resource's attributes when defining another. Terraform automatically works out the correct creation order from these references.

<CommandBlock command={`resource "aws_vpc" "main" {
  cidr_block = "10.0.0.0/16"
}

resource "aws_subnet" "public" {
  vpc_id     = aws_vpc.main.id    # reference the VPC
  cidr_block = "10.0.1.0/24"
}`} language="hcl" />

Because the subnet references `aws_vpc.main.id`, Terraform knows to create the VPC **first**. This dependency graph is built automatically — you never sequence things by hand.

<Tip title="Implicit dependencies">
Referencing another resource creates an *implicit dependency*. This is the preferred way to order resources — Terraform figures out the graph for you.
</Tip>

---

# 🧪 Hands-on Lab

<Exercise title="Parameterise a Config">
  <div slot="tasks">
    1. Create a variable `filename` with a default, used by a `local_file` resource
    2. Add an output that returns the file's content
    3. Override the filename with `-var` on apply
  </div>

  <div slot="solution">
    <CommandBlock command={`variable "filename" {
  type    = string
  default = "note.txt"
}

resource "local_file" "note" {
  filename = var.filename
  content  = "Environment: production"
}

output "content" {
  value = local_file.note.content
}`} language="hcl" />
    <Terminal
      command='terraform apply -auto-approve -var="filename=prod.txt" && terraform output content'
      output={`local_file.note: Creation complete after 0s
Apply complete! Resources: 1 added.
content = "Environment: production"`}
    />
  </div>
</Exercise>

---

# 🧠 Knowledge Check

<Quiz
  question="How do you reference an input variable named 'region' in your configuration?"
  options={["region", "var.region", "$region", "input.region"]}
  answer={1}
  explanation="Input variables are referenced with the var. prefix, e.g. var.region. Resource attributes use resource_type.name.attribute."
/>

<Quiz
  question="What happens when one resource references another resource's attribute?"
  options={[
    "Terraform errors out",
    "Terraform creates an implicit dependency and orders creation automatically",
    "You must manually specify creation order",
    "The reference is ignored"
  ]}
  answer={1}
  explanation="Referencing another resource creates an implicit dependency. Terraform builds a dependency graph and creates resources in the correct order automatically."
/>

---

# 💼 Interview Preparation

<InterviewPrep question="How would you use one Terraform configuration to manage multiple environments (dev, staging, prod)?">
  Several common approaches:

  - **Variable files per environment:** `dev.tfvars`, `prod.tfvars`, applied with `-var-file`.
  - **Workspaces:** `terraform workspace new prod` keeps separate state per environment from the same code.
  - **Directory or module structure:** a shared module called with different inputs per environment.

  The goal is always the same: **one source of truth**, parameterised by variables, so environments stay consistent and differ only where they must. Referencing outputs between configs (via remote state) lets larger systems compose cleanly.
</InterviewPrep>

---

# Summary

You can now parameterise configs with variables, return values with outputs, and wire resources together with references that build Terraform's dependency graph automatically. Next, we tackle the most important operational topic: state.

<NextLesson
  title="State & Remote Backends"
  href="/ultimate-devops-learning-hub/terraform/state-and-backends"
/>
