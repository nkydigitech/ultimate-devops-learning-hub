---
title: "Secrets & Deployment"
description: "Store credentials safely with GitHub Secrets and extend your pipeline into continuous deployment with protected environments."
order: 4
---

import LessonHeader from "../../components/LessonHeader.astro";
import CommandBlock from "../../components/CommandBlock.astro";
import Terminal from "../../components/Terminal.astro";
import Tip from "../../components/Tip.astro";
import Warning from "../../components/Warning.astro";
import Quiz from "../../components/Quiz.astro";
import InterviewPrep from "../../components/InterviewPrep.astro";
import Exercise from "../../components/Exercise.astro";
import NextLesson from "../../components/NextLesson.astro";

<LessonHeader
  title="Secrets & Deployment"
  description="From continuous integration to continuous deployment — done safely."
  timeToRead="11 min"
  icon="⚙️"
  section="GitHub Actions"
  sectionUrl="/github-actions/introduction"
/>

Deploying means using credentials — API keys, cloud tokens, passwords. Hard-coding those in a workflow would leak them to anyone who can read the repo. **GitHub Secrets** store them safely, and **environments** add approval gates. Together they turn CI into full CD.

---

# 🎯 Learning Objectives

By the end of this lesson you will:

- Store and use secrets securely in workflows
- Understand repository vs environment secrets
- Add a deploy job that runs only after tests pass
- Protect production with environment approval rules

---

# GitHub Secrets

Secrets are encrypted values you store in the repo settings (Settings → Secrets and variables → Actions). They're never printed in logs and can't be read back — only used.

Reference a secret with the `secrets` context:

<CommandBlock command={`steps:
  - name: Deploy
    run: ./deploy.sh
    env:
      API_TOKEN: \${{ secrets.API_TOKEN }}
      AWS_ACCESS_KEY_ID: \${{ secrets.AWS_ACCESS_KEY_ID }}`} language="yaml" />

<Warning title="Never echo a secret">
Don't `echo` a secret or write it to a file that gets uploaded. GitHub masks known secret values in logs, but careless handling (base64, string manipulation) can still expose them. Treat secrets as write-only.
</Warning>

---

# Repository vs Environment Secrets

| Scope | Where it applies |
|-------|------------------|
| **Repository secret** | Available to all workflows in the repo |
| **Environment secret** | Only available to jobs targeting that environment |

Environment secrets are more secure: your production database password lives only in the `production` environment, so a job that doesn't target production can't touch it.

---

# A Deploy Job

Extend the pipeline: deploy **only** after CI passes, and **only** from `main`:

<CommandBlock command={`jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: npm ci && npm test && npm run build

  deploy:
    needs: ci                                  # wait for CI to pass
    if: github.ref == 'refs/heads/main'        # only from main
    runs-on: ubuntu-latest
    environment: production                    # use the production environment
    steps:
      - uses: actions/checkout@v4
      - name: Deploy
        run: ./deploy.sh
        env:
          DEPLOY_TOKEN: \${{ secrets.DEPLOY_TOKEN }}`} language="yaml" />

The three guardrails working together:

- `needs: ci` — never deploy untested code.
- `if: github.ref == 'refs/heads/main'` — feature branches don't deploy.
- `environment: production` — unlocks production secrets and any approval rules.

<Terminal
  command="# In the Actions tab:"
  output={`✓ ci                     completed
● deploy                 waiting for approval
  Review required — production environment`}
/>

---

# Environment Protection Rules

On the `production` environment you can require:

- **Required reviewers** — a human must click "Approve" before deploy runs.
- **Wait timer** — a delay before deployment proceeds.
- **Branch restrictions** — only specific branches may deploy.

<Tip title="This is safe continuous deployment">
With a required reviewer on production, your pipeline deploys automatically to staging but pauses for a human "go" before touching production. That's the balance most teams want: fast, but with a safety catch on the highest-risk step.
</Tip>

---

# 🧪 Hands-on Lab

<Exercise title="Add a Guarded Deploy Stage">
  <div slot="tasks">
    1. Store a fake `DEPLOY_TOKEN` as a repository secret
    2. Add a `deploy` job that needs the CI job and only runs on `main`
    3. Target a `production` environment and use the secret as an env var
  </div>

  <div slot="solution">
    <CommandBlock command={`deploy:
  needs: ci
  if: github.ref == 'refs/heads/main'
  runs-on: ubuntu-latest
  environment: production
  steps:
    - uses: actions/checkout@v4
    - run: echo "Deploying with token"
      env:
        DEPLOY_TOKEN: \${{ secrets.DEPLOY_TOKEN }}`} language="yaml" />
    Add a required reviewer to the `production` environment so deploys pause for approval.
  </div>
</Exercise>

---

# 🧠 Knowledge Check

<Quiz
  question="How should credentials like an API token be handled in a GitHub Actions workflow?"
  options={[
    "Hard-coded directly in the YAML",
    "Stored as an encrypted GitHub Secret and referenced via the secrets context",
    "Committed to a config file",
    "Printed to the logs for reference"
  ]}
  answer={1}
  explanation="Credentials belong in encrypted GitHub Secrets, referenced with the secrets context. They're never hard-coded, committed, or echoed to logs."
/>

<Quiz
  question="Which combination ensures a deploy job only runs on tested code from the main branch?"
  options={[
    "Nothing — it always runs",
    "needs: ci  plus  if: github.ref == 'refs/heads/main'",
    "A matrix build",
    "A schedule trigger"
  ]}
  answer={1}
  explanation="needs: ci makes deploy wait for the CI job to pass, and the if condition restricts it to the main branch — together preventing untested or feature-branch deploys."
/>

---

# 💼 Interview Preparation

<InterviewPrep question="How do you design a safe deployment pipeline in GitHub Actions?">
  Layer the safeguards so speed never sacrifices safety:

  - **Secrets** stored encrypted, scoped to environments, never in code or logs.
  - **Gated ordering** — the deploy job `needs` the CI job, so only tested code ships.
  - **Branch conditions** — deploy only from `main` (or tags), never feature branches.
  - **Environments** — separate `staging` and `production`, each with its own secrets.
  - **Protection rules** — required reviewers and/or wait timers before production deploys.
  - **Rollback plan** — keep the previous artifact so you can redeploy it quickly.

  This delivers continuous deployment to staging automatically, with a controlled, auditable gate before production.
</InterviewPrep>

---

# Summary

You've completed the GitHub Actions track: CI/CD concepts, workflow anatomy, a full CI pipeline, and now secure secrets and gated deployment. You can automate the whole path from push to production safely. Next, we explore another major CI/CD tool used across the industry: Jenkins.

<NextLesson
  title="Introduction to Jenkins"
  href="/ultimate-devops-learning-hub/jenkins/introduction"
/>
